# Contact API (Rust + Axum + MySQL)
PORT=3001

# Hostinger MySQL — get these from Hostinger cPanel → MySQL Databases
# Format: mysql://USERNAME:PASSWORD@HOSTNAME:3306/DATABASE_NAME
DATABASE_URL=mysql://your_db_user:your_db_password@your_hostinger_host:3306/your_db_name

# CORS — comma-separated allowed origins (leave empty to allow all)
ALLOWED_ORIGINS=https://muhammadhasaan82.github.io

# Logging level (trace | debug | info | warn | error)
RUST_LOG=contact_api=info
