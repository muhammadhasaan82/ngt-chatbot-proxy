services:
  contact-api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3001:3001"
    # Database is hosted on Hostinger MySQL — no local DB container needed
